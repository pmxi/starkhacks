# Copy to frontend/.env and point at your ESP32's IP or mDNS hostname.
# The frontend opens a WebSocket to ws://<VITE_ESP_ID>/ws for sensor reps.
# Leave unset to disable the ESP integration (MediaPipe still works).
VITE_ESP_ID=192.168.1.50
