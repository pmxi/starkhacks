
  # SOLFIT

  This is a code bundle for SOLFIT. The original project is available at https://www.figma.com/design/yghspNeDhTiyGgL8KMeH1r/SOLFIT.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  